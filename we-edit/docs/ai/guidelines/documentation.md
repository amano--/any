# ドキュメント作成プロトコル（AI専用）

**注意**: このファイルはAI実行用に最適化されており、人間が読むことを意図していません。

## 1. 実装計画書プロトコル

```typescript
@protocol[implementation_plan]
{
  location: {
    base: "[機能名]/logs/ai/YYYY-MM",
    filename: "YYYY-MM-DD_HH_MM-実装計画名.md"
  },

  structure: {
    header: {
      title: "実装計画: [機能名]",
      created_at: "YYYY-MM-DD HH:MM"
    },
    
    sections: [
      "概要",
      "技術的アプローチ",
      "タスク分割",
      "考慮事項",
      "テスト計画"
    ]
  },

  references: {
    in_code: {
      format: "markdown_link",
      location: "file_top"
    },
    history: {
      format: "markdown_list",
      location: "file_bottom"
    }
  }
}
```

## 2. コメント構造プロトコル

```typescript
@protocol[comment_structure]
{
  component_structure: {
    tag: "@ai_component_structure",
    sections: [
      "レイアウト構成",
      "状態管理",
      "イベントハンドラ"
    ]
  },

  decision_process: {
    tag: "@ai_decision",
    sections: [
      "選択した実装アプローチ",
      "理由",
      "検討した代替案"
    ]
  },

  implementation_details: {
    tag: "@ai_implementation",
    sections: [
      "機能",
      "アルゴリズム",
      "パフォーマンス考慮"
    ]
  }
}
```

## 3. バージョン管理プロトコル

```typescript
@protocol[version_control]
{
  commit_format: {
    prefix: ["feat", "fix", "refactor", "docs", "test", "chore"],
    description: "max_50_chars",
    body: "bullet_points",
    footer: [
      "🤖 Generated by AI",
      "Co-Authored-By: Claude <noreply@anthropic.com>"
    ]
  },

  granularity: {
    scope: "single_feature",
    includes: [
      "implementation",
      "tests",
      "docs"
    ]
  }
}
```

## 4. 品質管理プロトコル

```typescript
@protocol[quality_control]
{
  review_checklist: [
    "implementation_plan_consistency",
    "decision_process_documentation",
    "performance_requirements",
    "security_considerations",
    "test_coverage",
    "documentation_updates"
  ],

  technical_debt: {
    tracking: "explicit_comments",
    prioritization: "impact_urgency_matrix",
    review_cycle: "sprint_based"
  }
}
```

## 5. ナレッジ管理プロトコル

```typescript
@protocol[knowledge_management]
{
  documentation: {
    location: {
      implementation_plans: "logs/ai/YYYY-MM",
      related_docs: "logs/ai/YYYY-MM",
      adr: "logs/ai/adr"
    },
    
    linking: {
      cross_references: true,
      version_tracking: true,
      dependency_mapping: true
    }
  },

  lessons_learned: {
    capture_timing: "post_implementation",
    categories: [
      "success_factors",
      "challenges",
      "improvements"
    ],
    sharing: "team_accessible"
  }
}
```

## 6. メトリクス収集プロトコル

```typescript
@protocol[metrics_collection]
{
  performance: {
    timing: "implementation_phases",
    benchmarks: "automated",
    comparisons: "historical_data"
  },

  quality: {
    code_coverage: "percentage",
    error_rates: "per_component",
    user_feedback: "structured"
  },

  documentation: {
    completeness: "checklist_based",
    accuracy: "peer_reviewed",
    accessibility: "role_based"
  }
}